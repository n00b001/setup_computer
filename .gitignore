# macOS / SMB AppleDouble sidecars
._*
.DS_Store

# tool + session operational state (not source)
.omc/
.remember/
.claude/
logs/

# timestamped backups written by setup.sh (e.g. ~/.zshrc.bak.<ts>)
*.bak.*

# cpp-builder outputs
cpp-builder/build/
**/build-*.log

# personal feedback/scratch notes
my-logs

# personal/org Claude config not for a public repo (travels with the folder)
claude/private/
.tenet/
